
pages:
    image: debian
    stage: deploy
    before_script:
       - apt-get update -qq
       - apt-get install -y -qq build-essential
    script:
        - mkdir -p public
        - cp index.html public/
    artifacts:
        paths:
            - public

